package com.github.chkypros.aoc2022.day6;

import java.util.ArrayDeque;
import java.util.List;

/**
 * @author <a href="mailto:kypros.chrysanthou@britebill.com">Kypros Chrysanthou</a>
 */
public class UniqueSequenceIdentifier {
    private final ArrayDeque<Character> lookback;

    public UniqueSequenceIdentifier(int lookbackDepth) {
        lookback = new ArrayDeque<>(lookbackDepth);
    }

    public int findUniqueSequenceIndex(List<Character> sequence) {
        int index = 0;
        while (lookback.addLast();)
        return 0;
    }
}
